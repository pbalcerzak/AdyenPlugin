sylius_grid:
    templates:
        filter:
            adyen_log_level: "@SyliusAdyenPlugin/admin/logger/grid/filter/level.html.twig"
    grids:
        sylius_adyen_admin_log:
            driver:
                name: doctrine/orm
                options:
                    class: "%sylius_adyen.model.log.class%"
            sorting:
                dateTime: desc
            fields:
                dateTime:
                    type: datetime
                    label: sylius_adyen.ui.logging.date_time
                    sortable: ~
                    options:
                        format: 'Y-m-d H:i:s'
                level:
                    type: twig
                    label: sylius_adyen.ui.logging.level
                    sortable: ~
                    options:
                        template: "@SyliusAdyenPlugin/admin/logger/grid/field/level.html.twig"
                token:
                    type: string
                    sortable: ~
                    label: token
                errorCode:
                    type: string
                    label: sylius_adyen.ui.logging.error_code
                    sortable: ~
                message:
                    type: string
                    label: sylius_adyen.ui.logging.message
            filters:
                date:
                    type: date
                    label: sylius_adyen.ui.logging.search
                    options:
                        field: dateTime
                        label: test
                level:
                    type: adyen_log_level
                    label: sylius_adyen.ui.logging.level
